<view type="DETAIL" model="auth.AuthRole" name="AuthRoleDetail" title="角色详情">
    <template slot="actions">
        <action name="$$internal_GotoListTableRouter" type="default"/>
    </template>
    <template slot="fields">
        <field data="id" invisible="true"/>
        <pack widget="group" title="角色信息" cols="2">
            <field data="code" label="角色编码"/>
            <field data="name" label="角色名称"/>
            <field data="roleType" label="角色类型"/>
            <field data="description" label="角色描述" colSpan="2"/>
        </pack>
        <pack widget="group" title="其他信息" cols="2">
            <field data="createUserName" label="创建人"/>
            <field data="createDate" label="创建时间"/>
            <field data="writeUserName" label="更新人"/>
            <field data="writeDate" label="更新时间"/>
        </pack>
    </template>
</view>
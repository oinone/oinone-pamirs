server:
  address: 0.0.0.0
  port: 8088
  sessionTimeout: 3600

spring:
  profiles:
    active: dev
  application:
    name: pamirs-compute

---
spring:
  profiles: dev
  cloud:
    config:
      enabled: false
      uri: http://127.0.0.1:7001
      label: master
      profile: dev
    nacos:
      config:
        enabled: false
        server-addr: http://127.0.0.1:8848
        file-extension: yml

